# Sessions — 2026-08-08

## 14:10 · ask · local-brain
**Asked:** how many email landed today

The provided context does not contain data on how many emails landed today. I cannot answer this question based on the available information.

## 15:31 · ask · local-triage
**Asked:** test

Based on the provided context, testing information is available in the following documents and identifiers:

**Documents:**
- `Chapter 6 - Test Plan & Report.txt`
- `CSS Solutions Test Script.xlsx`
- `iFinQ_Production_Investor_Readiness_Audit.md`

**Test Case IDs:**
- `Sm_01`: Metadata of a file/container
- `TC_RA17`: Test ability to export reports to Microsoft Office products
- `TC_RA18`: Test ability to generate tabular reports based on user defined criteria
- `TC_RA19`: Test ability to define time intervals in the reports

**Audit Findings:**
- `1 passed (1)` Vitest suite is a placeholder (`src/test/example.test.ts`)
- Browserslist data is `12 months` stale

**Responsibilities:**
- Each developer responsible for running adequate tests on their assigned unit.
- Overall test of the system will be responsibility of each group members collaborating on testing process as well as test outcomes.

**Testing Schedule:**
- Testing occurs at completion of each unit, again if initial testing failed.
- After integration phase more testing will occur.

## 15:32 · ask · local-brain
**Asked:** what are the autonomy tiers

- T0 read/analyse/summarise/search: autonomous.
- T1 create drafts (files in drafts/, mail drafts, calendar events, tasks, scaffolds): autonomous, visible, reversible.
- T2 send, delete, spend, or modify external state: BLOCKING approval dialog. Default Deny. 5-minute timeout = Deny. Dialog shows the action AND the source content requesting it. All decisions logged.

## 15:53 · ask · local-brain
**Asked:** what did I decide about model residency

Based on `policies.md`, your decision is:

"Residency is tiered, not mode-switched. The embedder and the 4B triage model are pinned with `lms load` — no TTL, never auto-evicted — because every scheduled job depends on them. Large models are never pre-loaded: naming one in a request JIT-loads it, auto-evict unloads the previous one, and idle TTL releases it. Worst case is 21.96 GiB against a ~26 GiB envelope, so overcommit is structurally impossible rather than something a script must remember. An idle large model is more expensive than an unloaded one: MLX allocates weights in Metal's shared storage mode, whose pages are pageable, so an idle model is compressed and every subsequent inference pays decompression. Measured 1 Aug: 24 duplicate model instances had accumulated, pushing compressed memory to 16.65 GiB with 1.17 GiB free. Clearing them returned it to 0.70 GiB compressed and 23.07 GiB headroom. `residency status` reports duplicates; `residency pin` clears them. Watch memory pressure, not free megabytes."

## 19:30 · ask · local-brain
**Asked:** what's my super balance

The provided context does not state your specific super balance. It only references general regulatory thresholds, such as accounts with a balance of less than $6,000 and a personal transfer balance cap between $1.6 million and $1.7 million for starting pensions from 1 July 2021. Please check your ATO online account or fund statement for your actual balance.
