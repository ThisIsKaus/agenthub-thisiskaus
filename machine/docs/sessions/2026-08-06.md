# Sessions — 2026-08-06

## 20:59 · ask · local-brain
**Asked:** embedder health probe

The provided context does not cover an embedder health probe. It only lists `StartupHealthCheck`, `KeyVaultHealthCheck`, and `BlobStorageHealthCheck` mapped to `/health`, `/health/live`, `/health/ready`, and `/health/startup`.

## 21:02 · ask · local-brain
**Asked:** embedder health probe

The provided context does not cover an embedder health probe. It only lists `StartupHealthCheck`, `KeyVaultHealthCheck`, and `BlobStorageHealthCheck` mapped to `/health`, `/health/live`, `/health/ready`, and `/health/startup`.

## 21:23 · ask · local-brain
**Asked:** embedder health probe

The provided context does not cover an embedder health probe. It only lists `StartupHealthCheck`, `KeyVaultHealthCheck`, and `BlobStorageHealthCheck` mapped to `/health`, `/health/live`, `/health/ready`, and `/health/startup`.

## 21:59 · ask · local-brain
**Asked:** embedder health probe

The provided context does not cover an embedder health probe. It only lists `StartupHealthCheck`, `KeyVaultHealthCheck`, and `BlobStorageHealthCheck` mapped to `/health`, `/health/live`, `/health/ready`, and `/health/startup`.

## 22:01 · ask · local-brain
**Asked:** embedder health probe

The provided context does not cover an embedder health probe. It only lists `StartupHealthCheck`, `KeyVaultHealthCheck`, and `BlobStorageHealthCheck` mapped to `/health`, `/health/live`, `/health/ready`, and `/health/startup`.

## 22:05 · ask · local-brain
**Asked:** embedder health probe

The provided context does not cover an embedder health probe. It only lists `StartupHealthCheck`, `KeyVaultHealthCheck`, and `BlobStorageHealthCheck` mapped to `/health`, `/health/live`, `/health/ready`, and `/health/startup`.
