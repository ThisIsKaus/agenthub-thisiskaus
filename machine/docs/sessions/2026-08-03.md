# Sessions — 2026-08-03

## 08:09 · ask · local-brain
**Asked:** embedder health probe

The provided context does not cover an embedder health probe. It only lists `StartupHealthCheck`, `KeyVaultHealthCheck`, and `BlobStorageHealthCheck` mapped to `/health`, `/health/live`, `/health/ready`, and `/health/startup`.

## 13:07 · ask · local-brain
**Asked:** embedder health probe

The provided context does not cover an embedder health probe. It only lists `StartupHealthCheck`, `KeyVaultHealthCheck`, and `BlobStorageHealthCheck` mapped to `/health`, `/health/live`, `/health/ready`, and `/health/startup`.

## 16:15 · ask · local-brain
**Asked:** embedder health probe

The provided context does not cover an embedder health probe. It only lists `StartupHealthCheck`, `KeyVaultHealthCheck`, and `BlobStorageHealthCheck` mapped to `/health`, `/health/live`, `/health/ready`, and `/health/startup`.

## 16:15 · ask · local-brain
**Asked:** embedder health probe

The provided context does not cover an embedder health probe. It only lists `StartupHealthCheck`, `KeyVaultHealthCheck`, and `BlobStorageHealthCheck` mapped to `/health`, `/health/live`, `/health/ready`, and `/health/startup`.

## 18:32 · ask · local-brain
**Asked:** embedder health probe

The provided context does not cover an embedder health probe. It only lists `StartupHealthCheck`, `KeyVaultHealthCheck`, and `BlobStorageHealthCheck` mapped to `/health`, `/health/live`, `/health/ready`, and `/health/startup`.

## 18:32 · ask · local-brain
**Asked:** embedder health probe

The provided context does not cover an embedder health probe. It only lists `StartupHealthCheck`, `KeyVaultHealthCheck`, and `BlobStorageHealthCheck` mapped to `/health`, `/health/live`, `/health/ready`, and `/health/startup`.

## 18:41 · ask · local-brain
**Asked:** embedder health probe

The provided context does not cover an embedder health probe. It only lists `StartupHealthCheck`, `KeyVaultHealthCheck`, and `BlobStorageHealthCheck` mapped to `/health`, `/health/live`, `/health/ready`, and `/health/startup`.

## 18:44 · ask · local-brain
**Asked:** embedder health probe

The provided context does not cover an embedder health probe. It only lists `StartupHealthCheck`, `KeyVaultHealthCheck`, and `BlobStorageHealthCheck` mapped to `/health`, `/health/live`, `/health/ready`, and `/health/startup`.

## 20:05 · ask · local-brain
**Asked:** what are my mortgage obligations in AUD

Based on the provided context, your specific mortgage obligations in AUD are:
- Loan outstanding: AUD $1,431,880
- Monthly mortgage repayment: AUD $8,670

These values are quoted verbatim from the "Findability Sciences - Detailed Analysis.pdf" property snapshot. The remaining documents outline general contractual terms, default conditions, and data-handling policies but do not list specific AUD amounts for your mortgage.

## 20:08 · ask · local-brain
**Asked:** embedder health probe

The provided context does not cover an embedder health probe. It only lists `StartupHealthCheck`, `KeyVaultHealthCheck`, and `BlobStorageHealthCheck` mapped to `/health`, `/health/live`, `/health/ready`, and `/health/startup`.
